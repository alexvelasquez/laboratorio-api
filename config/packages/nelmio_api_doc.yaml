nelmio_api_doc:
    documentation:
        info:
            title: Desarrollo de Sotfware en Sistemas Distribuidos
            description: API REST de DSSD
            version: 1.0.0
    routes: # to filter documented routes
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
    models: { use_jms: false }
